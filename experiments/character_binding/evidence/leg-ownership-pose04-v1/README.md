# Opposite-crossing follow-up: pose 4

One built-in `image_gen` request, following the approved pose-10 comparison.
Here the orange near leg A stands in front, while the blue far leg B folds
behind. The same stouter material guides and original mouse reference are used,
with the ownership diagram and wording changed to describe this actual pose.

The raw output, exact prompt, inputs, region guide, fixed-canvas mapping and
original-pixel composite are retained under `combined/`. It returned an opaque
1254×1254 image, mapped uniformly to 256px. No image fitting or new garment
motion was applied. The result preserves a standing foreground leg and a high
boot behind it, but the coat hides much of the crossing; this one sample is
not proof of anatomical or temporal consistency.

The combined review is `../leg-ownership-trial-v1/review.html`. Generation of
independent parts for pose 4 and a complete twelve-frame batch remain unrun.
