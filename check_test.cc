#include <iostream>
#include <map>     // Unused but commented out?
#include <string>  // Unused
#include <vector>  // This is used
// #include <set>

int main() {
  std::vector<int> v;
  std::cout << "Hello" << std::endl;
  return 0;
}
